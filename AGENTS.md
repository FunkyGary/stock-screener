# Repository Workflow

- Unless the user explicitly says otherwise, completed code or configuration
  changes should be committed and pushed to `origin/main`.
- Do not include local editor settings, caches, or generated data snapshots in
  feature commits unless the user specifically asks for them.
